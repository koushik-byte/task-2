<section id="about" class="py-5 bg-light border-top reveal">
  <div class="container">
    <div class="row align-items-center gy-4">
      <div class="col-lg-7">
        <h1 class="display-5 fw-bold mb-3">{{ about.title }}</h1>
        <p class="lead text-secondary">{{ about.description }}</p>
      </div>
      <div class="col-lg-5">
        <img src="{{ about.image }}" class="img-fluid rounded-4 shadow-sm"/>
      </div>
    </div>
  </div>
</section>
